spring:
  application:
    name: order-inventory-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true
        orm:
          jdbc:
            bind: TRACE

